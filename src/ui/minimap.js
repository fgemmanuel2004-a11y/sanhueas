/**
 * SANHUEAS - Minimap
 * Mapa pequeño que muestra al jugador y el mapa
 */

SANHUEAS.Minimap = {
  width: 100,
  height: 100,
  x: 10,
  y: 10,
  scale: 1,

  /**
   * Renderiza el minimap
   */
  render(ctx) {
    // Implementado en HUD.renderMinimap por ahora
  }
};
